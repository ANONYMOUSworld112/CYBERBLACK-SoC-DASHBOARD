export async function notifyEmail(_to, _subject, _body) {
  // Stub — wire to nodemailer in production
}

export async function notifyWebhook(_url, _payload) {
  // Stub — wire to fetch POST in production
}
